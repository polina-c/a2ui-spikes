import * as react from 'react';
import react__default from 'react';
import * as _a2ui_web_core_v0_9 from '@a2ui/web_core/v0_9';
import { ComponentNode, Signal, ComponentApi, ComponentContext, ResolveA2uiProps, InferredComponentApiSchemaType, SurfaceModel, Catalog } from '@a2ui/web_core/v0_9';
import { MarkdownRenderer } from '@a2ui/web_core/types/types';

/** Renders a resolved child node, or falls back for an unresolved id. */
type NodeBuildChild = (child: ComponentNode<ReactComponentImplementation> | string, basePath?: string) => react__default.ReactNode;
/** What a component implementation's `view` receives from the node surface. */
type NodeViewProps = {
    node: ComponentNode<ReactComponentImplementation>;
    buildChild: NodeBuildChild;
};
declare function useSignalValue<T>(signal: Signal<T>): T;

interface ReactComponentImplementation extends ComponentApi {
    /** The framework-specific rendering wrapper. */
    render: react__default.FC<{
        context: ComponentContext;
        buildChild: (id: string, basePath?: string) => react__default.ReactNode;
    }>;
    /**
     * Renders this component from a resolved `ComponentNode`. Generated by the
     * factories below. When absent, `A2uiSurface` renders through `render`,
     * handing it a `buildChild` that resolves ids through the node layer.
     */
    view?: react__default.FC<NodeViewProps>;
}
type ReactA2uiComponentProps<T> = {
    props: T;
    buildChild: (id: string, basePath?: string) => react__default.ReactNode;
    context: ComponentContext;
};
/**
 * Creates a React component implementation using the deep generic binder.
 */
declare function createComponentImplementation<Api extends ComponentApi>(api: Api, RenderComponent: react__default.FC<ReactA2uiComponentProps<ResolveA2uiProps<InferredComponentApiSchemaType<Api>>>>): ReactComponentImplementation;
/**
 * Creates a React component implementation that manages its own context bindings (no generic binder).
 */
declare function createBinderlessComponentImplementation(api: ComponentApi, RenderComponent: react__default.FC<{
    context: ComponentContext;
    buildChild: (id: string, basePath?: string) => react__default.ReactNode;
}>): ReactComponentImplementation;

/**
 * Surface renderer driven by the node layer.
 *
 * `A2uiSurface` constructs one `NodeResolver` and renders the resolved
 * `ComponentNode` tree it maintains. Each implementation carries a generated
 * `view` (see `adapter.tsx`) that subscribes to its own node's props and
 * converts them back to the shapes existing views expect, so a data change
 * re-renders exactly the affected component. The surface only dispatches: it
 * hands each `view` its node and a `buildChild` that renders resolved child
 * nodes.
 */

declare const A2uiSurface: react__default.FC<{
    surface: SurfaceModel<ReactComponentImplementation>;
}>;

/** Renders text content, using the Markdown pipeline for rich text or native HTML elements for known typographic variants. */
declare const Text: ReactComponentImplementation;

declare const Image: ReactComponentImplementation;

declare const Icon: ReactComponentImplementation;

declare const Video: ReactComponentImplementation;

declare const AudioPlayer: ReactComponentImplementation;

declare const Row: ReactComponentImplementation;

declare const Column: ReactComponentImplementation;

declare const List: ReactComponentImplementation;

declare const Card: ReactComponentImplementation;

declare const Tabs: ReactComponentImplementation;

declare const Divider: ReactComponentImplementation;

declare const Modal: ReactComponentImplementation;

declare const Button: ReactComponentImplementation;

declare const TextField: ReactComponentImplementation;

declare const CheckBox: ReactComponentImplementation;

declare const ChoicePicker: ReactComponentImplementation;

declare const Slider: ReactComponentImplementation;

declare const DateTimeInput: ReactComponentImplementation;

declare const MarkdownContext: react.Context<MarkdownRenderer | undefined>;
declare const useMarkdownRenderer: () => MarkdownRenderer | undefined;

declare const basicCatalog: Catalog<ReactComponentImplementation, _a2ui_web_core_v0_9.FunctionImplementation>;

export { A2uiSurface, AudioPlayer, Button, Card, CheckBox, ChoicePicker, Column, DateTimeInput, Divider, Icon, Image, List, MarkdownContext, Modal, type NodeBuildChild, type NodeViewProps, type ReactA2uiComponentProps, type ReactComponentImplementation, Row, Slider, Tabs, Text, TextField, Video, basicCatalog, createBinderlessComponentImplementation, createComponentImplementation, useMarkdownRenderer, useSignalValue };
