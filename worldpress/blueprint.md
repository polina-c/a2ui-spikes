# Minimal worldpress AI site

`worldpress/minimal` contains code and steps to run locally a minimal serverless
website that:

1. Uses https://wordpress.org/plugins/ai-engine/ 
2. Shows a chat with ai to user

`worldpress/minimal/README.md` describes the steps to run the site locally.
